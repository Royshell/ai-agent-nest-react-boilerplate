{
  "name": "ai-agent-nest-react-boilerplate",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo boilerplate for an AI agent built with TypeScript, NestJS and React.",
  "scripts": {
    "lint": "echo \"No lint configured yet\"",
    "build": "echo \"No build configured yet\"",
    "dev": "echo \"Apps are not set up yet. Run dev scripts from each app once created.\""
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.0.0"
  }
}