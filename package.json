{
  "name": "ai-agent-nest-react-boilerplate",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo boilerplate for an AI agent built with TypeScript, NestJS and React.",
  "scripts": {
    "lint": "echo \"No lint configured yet\"",
    "build": "echo \"No build configured yet\"",
    "dev": "concurrently -n SERVER,WEB -c blue,green \"pnpm --filter server start:dev\" \"pnpm --filter web dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
